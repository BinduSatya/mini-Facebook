<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="min-h-screen bg-zinc-900 text-white pt-10 px-10 pb-1">
      <b
        ><a href="/logout" class="text-red-800 bg-white px-5 py-3 rounded-md"
          >Logout</a
        ></b
      >
      <div class="text-3xl pb-1 mt-5">Hello <%= user.name %> 👋</div>
      <div class="text-sm text-zinc-400 mb-10">here you can create a post</div>
      <form action="/create-post" method="post">
        <textarea
          name="textarea"
          id="textarea"
          placeholder="Write whats on your mind"
          class="w-1/3 bg-transparent border-zinc-500 block p-4 border-2 resize-none mb-4 rounded-md"
        ></textarea>
        <button
          type="submit"
          class="bg-blue-500 text-b px-5 py-3 m-0text-black rounded-md block"
        >
          Create a post
        </button>
      </form>
      <div class="posts mt-20">
        <h3 class="text-white">Your posts.</h3>
        <div class="container gap-3">
          <%user.posts.forEach((post)=>{%>
          <div class="postcontainer mt-5 ml-5">
            <div class="post w-1/3 p-4 rounded-md bg-zinc border-2 bg-zinc-800">
              <h4 class="text-blue-500 mb-2">@<%=user.username%></h4>
              <p class="text-sm tracking-tight"><%=post.content%></p>
              <div class="pt-4 flex gap-3">
                <a class="text-blue-500" href="">Like</a>
                <a class="text-zinc-500" href="">edit</a>
              </div>
            </div>
          </div>
          <%})%>
        </div>
      </div>
    </div>
  </body>
</html>
